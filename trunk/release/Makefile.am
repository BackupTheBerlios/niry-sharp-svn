SUBDIRS = src
EXTRA_DIST = niry-sharp.pc.in

pkgconfigdir = $(libdir)/pkgconfig
pkgconfig_DATA = niry-sharp.pc

DISTCLEANFILES = config.guess config.sub Makefile.in configure COPYING INSTALL install-sh missing aclocal.m4
